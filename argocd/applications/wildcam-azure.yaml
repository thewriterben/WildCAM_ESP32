apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wildcam-azure-production
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: wildcam-multicloud
  
  source:
    repoURL: https://github.com/thewriterben/WildCAM_ESP32.git
    targetRevision: main
    path: helm/wildcam
    helm:
      releaseName: wildcam-azure
      values: |
        global:
          environment: production
          cloudProvider: azure
        
        backend:
          replicaCount: 5
          resources:
            requests:
              cpu: 1000m
              memory: 2Gi
            limits:
              cpu: 4000m
              memory: 8Gi
        
        multiCloud:
          azure:
            region: westus2
            storage:
              account: wildcamdataazure
        
        ingress:
          hosts:
            - host: azure.wildcam.example.com
              paths:
                - path: /
                  pathType: Prefix
  
  destination:
    server: https://wildcam-azure-aks.westus2.azmk8s.io
    namespace: wildcam-production
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  
  revisionHistoryLimit: 10
