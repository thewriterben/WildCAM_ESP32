# IMX500 Wildlife Detector Requirements
# For Arducam Pivistation 5 integration

# Note: Some dependencies are Raspberry Pi specific and may not install on other platforms
# For development/testing on non-Pi systems, comment out the Pi-specific dependencies

# Core dependencies
python-dateutil>=2.8.0

# Optional: MQTT support (recommended)
paho-mqtt>=1.6.1

# Optional: LoRa support (requires hardware)
# pyLoRa>=0.4.0  # Uncomment if using LoRa hardware

# Optional: HTTP/REST support
requests>=2.28.0

# Optional: Camera support (Raspberry Pi only)
# picamera2>=0.3.0  # Uncomment on Raspberry Pi with camera
# libcamera>=0.1.0  # Uncomment on Raspberry Pi

# Optional: IMX500 SDK (Arducam provided)
# Note: Install from Arducam's repository:
# pip install arducam-imx500-sdk

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
